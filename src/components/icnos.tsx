import {
  type LucideIcon,
  X,
  PlusCircle,
  AlertCircle,
} from 'lucide-react';

export type Icon = LucideIcon;

export const Icons = {
  x: X,
  plusCircle: PlusCircle,
  alertCircle: AlertCircle,
};
